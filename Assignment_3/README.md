TFTP Server


- Server

int main() {

    init();
    listen();
    // if connection
    parse();
    if put
        decline()
    else if get
 
        fork() {
            // create a new socket;
            close(fd) // close 69
            // serve the request;
            while(1)
                senddata();
                wait for the ack();

        }
}