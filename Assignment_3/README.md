TFTP Server


- Server

int main() {

    init();
    listen();
    // if connection
    fork() {
        // create a new socket;
        close(fd) // close 69
        parse();
        if put
            decline()
        else if get
            // serve the request;
            senddata();
    }
}